http:
  default_version: HTTP/1.1
  recv_timeout_ms: 5000
  max_connections: 1000

virtual_hosts:
  - hostnames: [example.com]
    path: /var/www
    interfaces:
      - address: 127.0.0.1
        port: 8080

plugins:
  enabled: true
  path: ./plugins
  config_dir: ./config
